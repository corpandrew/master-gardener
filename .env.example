GEMINI_API_KEY=your_gemini_api_key
GEMINI_MODEL=gemini-2.0-flash
# Optional custom endpoint if you need one:
# GEMINI_ENDPOINT=https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent

# Optional shared secret for Pi -> ingest auth.
INGEST_SHARED_TOKEN=replace_with_shared_secret

# Optional base64 character limit for the ingest endpoint.
MAX_IMAGE_BASE64_CHARS=7000000
